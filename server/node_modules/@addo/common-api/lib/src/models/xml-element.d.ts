export declare class XmlElement {
    mapping: MappingElement[];
    getXml(invoice_xml: string): string;
    private convertXml;
}
export declare class MappingElement {
    key: string;
    transform: (value: any) => any;
    constructor(key: string, transform?: (value: any) => any);
}
