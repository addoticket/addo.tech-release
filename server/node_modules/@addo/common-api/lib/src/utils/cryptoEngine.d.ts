export declare class CryptoEngine {
    private secret_key;
    private iv;
    constructor(secret_key?: string, iv?: string);
    encrypt(payload: string): string;
    decrypt(payload: string): string;
}
