export declare class HttpError extends Error {
    message: string;
    body: any;
    code: number;
    constructor(message: string, body: any, code?: number);
    static HandleError(err: any | HttpError): any;
}
