import { Context, APIGatewayEvent } from "aws-lambda";
import { Microservice } from "../../microservice";
import { Response } from "../../models/response";
import { HttpRequest } from "../../models/http.request";
export declare class NestJsMicroservice<T, R> extends Microservice<T, R> {
    context: Context;
    callback: (...args: any[]) => void;
    constructor(handler: (http: HttpRequest<T>) => Promise<Response<R>>);
    parseRequest(request: any): HttpRequest<T>;
    getAHandler(event: APIGatewayEvent, context: Context, callback: (...args: any[]) => void): Promise<Response<R>>;
}
