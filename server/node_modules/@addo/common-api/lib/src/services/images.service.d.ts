import * as AWS from "aws-sdk";
import "reflect-metadata";
export declare class ImagesService {
    private bucket;
    private accessKeyId;
    private secretAccessKey;
    private region;
    private s3;
    constructor();
    getImage(key: string): Promise<AWS.S3.GetObjectOutput>;
    addImage(key: string, body: any): Promise<string>;
    deleteImage(key: string): Promise<void | AWS.S3.DeleteObjectOutput>;
}
